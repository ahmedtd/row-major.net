stages:
  - deploy

deploy to prod kubernetes:
  stage: deploy
  image: docker:1.11
  services:
    - docker:dind
  script:
    - sudo apt-get install make python3 python3-jinja2
    - docker version
    - make deploy-gke
